<div class="row">
  <div class="col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3 text-center">
    <div class="page-header">
      <h2>Let’s start the licensing process.</h2>
      <p>We’re here to help you every step of the way.</p>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-3 text-center">
    <p class="text-greeting"> <strong>First, tell us where you want to open your school so we can pull up the right information.</strong></p>

    {{state-selector state=model.state onchange=(action (mut model.state))}}

    {{#liquid-bind (obj state=model.state regs=model.stateRegulation) use="fade" as |current|}}
      {{#if current.regs}}
        <a role="button" onclick={{action changeStep 1}} class="btn btn-default">Continue</a>
      {{else if current.state}}
        <div class="alert alert-warning" role="alert">We don't have regulations for this state yet. Please <a href="http://wildflowerschools.org/contact">get in touch</a> so we can work together to add them.</div>
      {{else}}
      {{/if}}
    {{/liquid-bind}}

    {{#post-it target=".ember-power-select" targetAttachment="top right" attachment="top left"}}
      <p>
        We're using browser local storage, so if you return to this page
        at any time, you will still see your previous entries.
      </p>
      <p>
        Consider adding UI to allow users to "Start From Scratch".
      </p>
    {{/post-it}}

  </div>
</div>
